spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

# S3 설정은 prod 프로필에서만 활성화 (@Profile("!local"))
# 로컬에서 S3 테스트가 필요하면 application-local.yaml에 cloud.aws 설정 추가